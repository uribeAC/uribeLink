import type React from "react";

const Layout: React.FC = () => {
  return (
    <div className="container min-h-screen min-w-screen h-14 bg-linear-to-t from-sky-500 to-indigo-500"></div>
  );
};

export default Layout;
