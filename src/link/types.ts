export interface Link {
  text: string;
  iconUrl: string;
  link: string;
}

export interface Profile {
  profileUrl: string;
  name: string;
  profession: string;
  location: string;
  description: string;
}
